version: '3'

services:
    server:
        build: packages/server
        user: "${UID}:${GID}"
        volumes:
            - ".:/app"
            - "/etc/passwd:/etc/passwd:ro"
        command: "node packages/server/index.js"
        environment:
            - HOME=/app/var/cache
